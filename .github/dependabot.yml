version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: pip
    directory: /
    schedule:
      interval: weekly
    ignore:
      # Mirrors the version Home Assistant pins, not the latest release.
      - dependency-name: async-upnp-client
