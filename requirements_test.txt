pytest-homeassistant-custom-component==0.13.366
# Home Assistant's ssdp component needs this to import in tests, so the pin
# mirrors what Home Assistant itself pins. Bump it when Home Assistant does.
async-upnp-client==0.48.1
pyytlounge==3.4.0
ruff==0.16.8
